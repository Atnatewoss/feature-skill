This repository is an **Agent Skill** for AI coding assistants.

When the user asks to implement a new feature, add functionality, or change code:

1. Read `SKILL.md` and consult the **Routing Table** to determine which developer profiles match the task domain.
2. Load the relevant profile(s) from `profiles/`.
3. Execute the `workflows/new-feature.md` workflow chain.
